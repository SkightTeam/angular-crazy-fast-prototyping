$ npm install -g cordova ionic
$ ionic start myApp tabs
* Customized App details in config.xml (do you want to touch on that?)
* Also we wont be customizing any scss files so we wont use Gulp, do you want to touch on Gulp at all?
$ ionic serve
$ ionic platform add ios (should we mention `ionic platfor list` at all?)
$ ionic ios run
Use existing files as template for creating our new view
Delete files/states we aren't using

